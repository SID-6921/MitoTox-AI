# data
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

# models - trained Step 2 artifacts are committed (fixed seed=42, reproducible
# via scripts/07-09, but small enough and valuable enough as deliverables to
# track directly rather than regenerate-only like data/raw and data/processed)

# python
__pycache__/
*.pyc
.ipynb_checkpoints/
venv/
.venv/
env/

# os
.DS_Store
Thumbs.db
